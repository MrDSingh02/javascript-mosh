// > d 
// ! Error or Bug
// ? Question
// / Comment hidden
// - 


let interestRate = 0.3;
interestRate = 1;

console.log(interestRate);

// > Constants
// - to keep the value unchanged if value changed it can produce error

// ! Bug or Error
const bugCount = 5;
bugCount = 100;
